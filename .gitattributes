# Force LF line endings for shell scripts.
# These scripts run on the production Linux VPS - keep LF on commit so
# checkouts on any platform don't break the shebang line.
*.sh                text eol=lf
sandbox/entrypoint.sh text eol=lf
