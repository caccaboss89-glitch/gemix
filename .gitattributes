# Force LF line endings for shell scripts and the bridge directory.
# Without this, git on Windows can convert *.sh to CRLF on checkout, which
# breaks the shebang line on the Linux VPS where the scripts actually run.
*.sh                text eol=lf
bridge/**           text eol=lf
sandbox/entrypoint.sh text eol=lf
