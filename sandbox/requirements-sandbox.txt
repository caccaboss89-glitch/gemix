# GemiX Python Sandbox — pre-installed libraries.
# Install with: pip install -r requirements-sandbox.txt
#
# NOTE: the sandbox has NO free internet access. Only controlled backend APIs
# (polygon-api-client for finance, astropy data servers for astronomy) are
# reachable through the proxy. Every other network call will fail — use the
# AI's dedicated web_search / browse_page / read_url_content tools instead.

# ── Kernel runtime (required) ──
jupyter_server==2.14.2
jupyter_client==8.6.3
ipykernel==6.29.5
ipython==8.29.0

# ── Core mathematical & scientific computing ──
numpy==2.1.3
scipy==1.14.1
sympy==1.13.3
mpmath==1.3.0

# ── Data processing ──
pandas==2.2.3

# ── Visualization ──
matplotlib==3.9.2
seaborn==0.13.2
plotly==5.24.1

# ── Image manipulation ──
Pillow==11.0.0
rembg==2.0.59
onnxruntime==1.20.1        # rembg backend (CPU only; installs no CUDA)
cairosvg==2.7.1
pytesseract==0.3.13

# ── Audio & video processing ──
pydub==0.25.1
librosa==0.10.2.post1
moviepy==1.0.3
imageio-ffmpeg==0.5.1      # bundled ffmpeg locator used by moviepy

# ── Physics & astronomy ──
astropy==6.1.5
qutip==5.0.4

# ── Finance ──
polygon-api-client==1.14.5

# ── Document creation ──
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2
reportlab==4.2.5

# ── Networking helper (used by AI for controlled APIs, must respect proxy) ──
requests==2.32.3
