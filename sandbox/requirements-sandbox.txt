# GemiX Python Sandbox - pre-installed libraries.
# Install with: pip install -r requirements-sandbox.txt
#
# NOTE: the sandbox has NO free internet access. Only allowlisted hosts
# (YouTube, X, Instagram, TikTok, Facebook) are reachable via the HTTP egress
# proxy for curl/wget. Build-agent yt-dlp runs on the VPS host (tailsocks), not
# here. Other network calls fail — use web_search / browse_page / read_url_content.

# -- Kernel runtime (required) --
jupyter_server==2.14.2
jupyter_client==8.6.3
ipykernel==6.29.5
ipython==8.29.0

# -- Core mathematical & scientific computing --
numpy==2.1.3
scipy==1.14.1
sympy==1.13.3
mpmath==1.3.0

# -- Data processing --
pandas==2.2.3

# -- Visualization --
matplotlib==3.9.2
seaborn==0.13.2
plotly==5.24.1

# -- Image manipulation --
Pillow==11.0.0
rembg==2.0.59
onnxruntime==1.20.1        # rembg backend (CPU only; installs no CUDA)
cairosvg==2.7.1

# -- Document creation & PDF processing --
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2
reportlab==4.2.5
pypdf==4.2.0
pdfplumber==0.11.4        # PDF text/table/structure extraction (pdf skill)
pdf2image==1.17.0         # render PDF pages to images via poppler (pdf skill)
jinja2==3.1.4
PyYAML==6.0.2

# -- Networking helper (used by AI for controlled APIs, must respect proxy) --
requests==2.32.3
# yt-dlp: NOT installed in container — build agent runs it on VPS host (tailsocks).
