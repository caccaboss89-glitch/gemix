# Ignore the whole project except the sandbox directory
# Only the sandbox/ tree is copied into the Docker image
# The rest of the repository is mounted at runtime

# Include the entire repository context, then whitelist the sandbox subtree
*
!sandbox/